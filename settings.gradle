pluginManagement {
    repositories {
        google()
        mavenCentral()
        gradlePluginPortal()
    }
}
dependencyResolutionManagement {
    repositoriesMode = "fallback"
    repositories {
        google()
        mavenCentral()
    }
}
rootProject.name = "BLEGuard"
include ':app'
